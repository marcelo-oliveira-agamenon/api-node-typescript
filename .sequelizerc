const path = require('path');

module.exports = {
  config: path.resolve(__dirname, 'src', 'database', 'config', 'database.json'),
  'migrations-path': path.resolve(__dirname, 'src', 'database', 'migrations'),
};
